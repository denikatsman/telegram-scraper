# Telegram Scraper audit launch record

Verified project: `/Users/xxx/GitHub/telegram-scraper`. All eight tasks use this existing folder with GPT-6 Astra, Max reasoning.

`source-baseline.json` records every tracked and non-ignored untracked file present before launch, its SHA-256 checksum, the saved Git state, the exact eight prompts, and version-selection evidence. Numbered task JSON files record the launch and report destination. `tasks.json` and `../AUDIT-TASKS.md` provide the combined index after launch.

Preserve the eight report files in `bug-audit/` before any task or project cleanup. No temporary worktrees are used.
