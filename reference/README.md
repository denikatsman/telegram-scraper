# Preserved comparison source

`ict-viper/` holds the original seven files from the Desktop folder named
`Telegram-Post-Scraper ICT_Viper`. The files were moved here unchanged so the
next review can compare ideas and implementation with the current app.

This reference is excluded from Git and distribution packages. It is not
imported or executed by Channel Archive. A local code review found no additional
features worth importing and no saved channel data. The scripts contain old
embedded API credentials, so the original reference remains local and ignored.
